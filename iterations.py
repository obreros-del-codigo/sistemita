import main

def foo_iterations():
    x = Symbol("x")
    y=sympify(formulae_str)
    yprime=y.diff(x)
    f=lambdify(x,y,"numpy")

    range=range(7)

    xl=[]
    yl=[]
    zl=[]
    for i in range:
        zf = f(zf)
        zl.append(zf)
    for i in range:
        xf = f(xf)
        xl.append(xf)
    for i in range:
        yf = f(yf)
        yl.append(yf)
    return iterations
